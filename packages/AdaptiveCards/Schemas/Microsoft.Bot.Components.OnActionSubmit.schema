{
    "$schema": "https://schemas.botframework.com/schemas/component/v1.0/component.schema",
    "$role": [ "implements(Microsoft.ITrigger)", "extends(Microsoft.OnCondition)" ],
    "title": "Action.Submit (message activity)",
    "description": "Actions to perform when an Adaptive Card is submitted via Action.Submit.",
    "type": "object",
    "required": [
      "actions"
    ]
}
