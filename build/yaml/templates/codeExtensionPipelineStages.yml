# TODO: NPM pipeline when sample is available

stages:
- stage: Build_pack
  displayName: Build and package
  jobs:
    - job: Build
      steps:
      - template: ../tasks/dotnetPackageSteps.yml
      - template: ../tasks/publishBuildArtifactsNugetSteps.yml