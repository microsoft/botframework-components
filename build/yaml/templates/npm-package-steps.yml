steps:
- task: NodeTool@0
  displayName: 'Install Node 14.x'
  inputs:
    versionSpec: '14.x'

- script: |
    npm install
  displayName: 'Install dependencies'
  workingDirectory: '$(WorkingDirectory)'

- powershell: |
    # Get existing version from package.json
    $packageJson = Get-Content package.json;
    $packageJsonData = $packageJson | ConvertFrom-Json;

    "Appending version $($packageJsonData.version) and suffix $(VersionSuffix)";
    $version = $($packageJsonData.version) + "." + "$(VersionSuffix)";

    # Replace matched version
    (Get-Content package.json) `
        -replace "(?<=""version""\:\s*"")([\d]+)([\.]{1}[\d]+)([\.]{1}[\d]+)([\.]{1}[\d]+)*(?="",)", "$version"` |
      Out-File package.json
  displayName: 'Replace version in package.json'
  condition: ne(variables.DeploymentRing, 'stable')
  workingDirectory: $(WorkingDirectory)

- script: |
    npm pack
  displayName: 'Package package'
  workingDirectory: '$(WorkingDirectory)'

- task: CopyFiles@2
  displayName: 'Copy npm files'
  inputs:
    SourceFolder: '$(WorkingDirectory)'
    Contents: '*.tgz'
    TargetFolder: '$(Build.ArtifactStagingDirectory)\npm'

- task: PublishBuildArtifacts@1
  displayName: 'Publish the npm package'
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)\npm'
    ArtifactName: 'npm'
    publishLocation: 'Container'