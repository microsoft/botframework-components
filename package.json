{
  "name": "@microsoft/botframework-components",
  "private": true,
  "workspaces": [
    "generators/*",
    "packages/*",
    "skills/declarative/*",
    "testing/*"
  ],
  "scripts": {
    "lint": "yarn workspaces foreach run lint"
  }
}
