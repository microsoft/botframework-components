{
  "name": "@microsoft/botframework-components",
  "private": true,
  "workspaces": [
    "generators/*",
    "packages/*",
    "packages/*/js",
    "skills/declarative/*",
    "testing/*"
  ],
  "scripts": {
    "lint": "yarn workspaces foreach run lint"
  }
}
